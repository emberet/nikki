import { icon } from "./app-shell";
export function studioTabs() {
  return `<div class="studio-tabs" role="group" aria-label="Studio sections"><button data-studio-tab="channel" aria-pressed="true">01 <span>Channel</span></button><button data-studio-tab="token" aria-pressed="false">02 <span>Creator token</span></button><button data-studio-tab="activity" aria-pressed="false">03 <span>Activity</span></button></div>`;
}
export function artworkEditor() {
  return `<section class="artwork-editor card" data-studio-panel="channel"><div class="section-heading"><div><span class="eyebrow accent">THE FINISHING TOUCH</span><h2>Put a face to it.</h2></div><a id="my-channel-link" class="btn" href="/creators/" hidden>View channel ↗</a></div><p id="artwork-note">Save your channel to add a photo and cover.</p><div class="artwork-grid">${["avatar", "banner"].map((kind) => `<div><div id="art-${kind}" class="art-preview ${kind}"><span>${kind === "avatar" ? "YOU" : "YOUR SPACE"}</span></div><label class="btn upload-label">${kind === "avatar" ? "Choose profile photo" : "Choose cover image"}<input type="file" data-art-upload="${kind}" accept="image/jpeg,image/png,image/webp" disabled></label><button class="btn btn-small" data-art-remove="${kind}" hidden>Remove ${kind === "avatar" ? "photo" : "cover"}</button></div>`).join("")}</div><p class="field-help">Your image is cropped from the center and resized before upload. Preview it above; you can replace it anytime. Channel artwork is separate from token artwork.</p></section>`;
}
export function activityPanel() {
  return `<section data-studio-panel="activity" class="activity-panel"><div class="section-heading"><div><span class="eyebrow accent">A LITTLE PEACE OF MIND</span><h2>Your activity.</h2></div><button class="btn" data-refresh-activity>Refresh ↻</button></div><p>Launches, fee collections, and their current status. Checking a transaction never creates a new one.</p><div id="activity-list" data-private aria-live="polite"></div><a class="text-link" href="/help/">Something stuck? Get help ↗</a></section>`;
}
export function helpPage() {
  return `<section class="creator-heading"><div><span class="eyebrow accent">LET’S SORT IT OUT</span><h1>A hand when<br>you need one<span class="accent">.</span></h1><p>Account questions, transaction trouble, or something that needs a closer look.</p></div></section><div class="help-grid"><section><form id="support-form" class="card"><h2>Talk to Nikki</h2><p>Sign in with your wallet to send a private request and read the reply here.</p><label for="support-category">What’s happening?</label><select name="category" id="support-category">${["Account help", "Transaction help", "Impersonation", "Misleading token claims", "Video report", "Community report", "Other"].map((s) => `<option>${s}</option>`).join("")}</select><label for="support-handle">Channel handle <span class="muted">(optional)</span></label><input name="handle" id="support-handle" maxlength="24" placeholder="creator_name"><label for="support-message">Tell us a little more</label><textarea id="support-message" name="message" minlength="15" maxlength="2000" rows="6" placeholder="What happened, and what were you trying to do? A public transaction signature or video link can help." required></textarea><p class="field-help">Never include a seed phrase, private key, password, or X credentials.</p><button class="btn btn-primary" type="submit">Send request ↗</button><p id="support-feedback" role="status"></p></form><section class="section-space"><div class="section-heading"><h2>Your requests</h2><button class="btn btn-small" data-refresh-support>Refresh ↻</button></div><div id="support-list" data-private aria-live="polite"></div></section></section><aside class="help-faq"><span class="eyebrow accent">GOOD TO KNOW</span><details open><summary>How do subscriptions work?</summary><p>Hold any positive amount of a creator’s verified token in your connected wallet. Selling it all ends your holder status. Every preserved video remains free to watch.</p></details><details><summary>What is free in Communities?</summary><p>Importing an existing Solana token, joining a community, and posting text have no Nikki fee or blockchain transaction. Text posts are stored on Nikki and can be removed. Permanent video storage is separate.</p></details><details><summary>My transaction is still confirming.</summary><p>Open Studio → Activity and check the original transaction. A slow confirmation does not mean you need to launch again.</p></details><details><summary>I’m on my phone.</summary><p>Open Nikki inside Phantom or Solflare using the wallet chooser. Keep X verification in that same browser. If you return to another browser, reopen the original wallet browser and verify again.</p></details><details><summary>Can I upload a video yet?</summary><p>The archive starts with the founder’s first video. Public video submissions and NIKKI governance will open in a later release.</p></details><details><summary>What does a report do?</summary><p>Your report enters Nikki’s private review queue. A report does not delete a preserved video or reverse a blockchain transaction.</p></details></aside></div>`;
}
export function libraryPage() {
  return `<section class="creator-heading"><div><span class="eyebrow accent">GOOD THINGS ARE WORTH COMING BACK TO</span><h1>Your library<span class="accent">.</span></h1><p>Saved videos and where you left off, linked to your signed-in wallet.</p></div><button class="btn" data-refresh-library>Refresh ↻</button></section><div id="library-results" data-private aria-live="polite"><div class="empty-state"><h2>Keep a place for the good stuff.</h2><p>Sign in to save videos and continue watching across devices.</p><button class="btn btn-primary" data-connect>Connect wallet ↗</button></div></div>`;
}
export function opsPage() {
  return `<section class="creator-heading"><div><span class="eyebrow accent">FOUNDER DESK</span><h1>Keep Nikki running<span class="accent">.</span></h1><p>Support, transaction recovery, and service checks.</p></div><button class="btn" data-refresh-ops>Refresh ↻</button></section><div id="ops-results" data-private aria-live="polite"><div class="notice">Sign in with the founder wallet to open operations.</div></div>`;
}
export function shareButtons(url: string) {
  return `<div class="btn-row"><button class="btn" data-share-url="${url}">${icon("share")} Share</button><button class="btn" data-copy="${url}">Copy link</button></div>`;
}
